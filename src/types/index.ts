/**
 * Shared TS types and interfaces entry point.
 *
 * No feature types exist yet — each feature ticket owns its own
 * `src/features/<feature_name>/types.ts`; only cross-feature/shared
 * contract types belong here.
 */
export {}
